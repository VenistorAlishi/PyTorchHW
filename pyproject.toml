[project]
name = "pytorch-hw"
version = "0.1.0"
description = "Homework: numpy modules and experiments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "matplotlib>=3.8",
    "scikit-learn>=1.3",
    "torch>=2.0",
]
